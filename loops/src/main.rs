fn main() {
    for number in (1..4).rev() {
        println!("{number}!");
    }
    println!("LIFTOFF!!!");

    for number in (1..4).rev() {
        println!("{number} and");
    }

    for thing in (1..20).rev() [
        println!("I'm still looping");
    ]
}
